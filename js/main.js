const navToggle=document.getElementById('nav-toggle'),navWrap=document.querySelector('.nav-wrap'),navLinks=document.querySelectorAll('.nav a');if(navToggle){navToggle.addEventListener('click',()=>{const open=navWrap.classList.toggle('open');navToggle.setAttribute('aria-expanded',open)})}navLinks.forEach(link=>link.addEventListener('click',()=>navWrap.classList.remove('open')));const progress=document.getElementById('scroll-progress');window.addEventListener('scroll',()=>{const h=document.documentElement.scrollHeight-window.innerHeight;progress.style.width=(h>0?(window.scrollY/h)*100:0)+'%'},{passive:true});